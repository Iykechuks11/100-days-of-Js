// Challenge 1

const getCelsius = (f) => {
  const celsius = ((f - 32) * 5) / 9;
  return celsius;
};

console.log(getCelsius(32));

// Challenge 2

const minMax = (...numbers) => {
  return Math.max();
};

console.log(minMax(1, 2, 3, 4, 5));
